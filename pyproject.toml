[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "spyne-execution"
requires-python = ">=3.8"
version = "0.1.0"
description = "Python bindings for Spyne execution engines"
readme = "README.md"

[tool.maturin]
features = []
module-name = "spyne_execution"

