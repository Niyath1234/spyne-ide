# Cargo configuration for Spyne IDE
# Uses vendored dependencies, but allows specific packages from crates.io via patches

[source.crates-io]
replace-with = "vendored-sources"

[source.vendored-sources]
directory = "vendor"
